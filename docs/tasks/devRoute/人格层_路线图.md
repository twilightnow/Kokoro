---
tags:
  - Kokoro
  - tasks
  - roadmap
  - 人格层
status: active
created: 2026-04-20
---

# 人格层路线图

← [返回总览](开发路线图.md)

设计参考：[人格层设计](../../desgin/人格层.md)

---

## 模块职责

人格层是整个项目的核心，也是最需要持续打磨的部分。

- **负责**：角色配置加载、情绪状态管理、prompt 组装、输出约束（禁用词/口癖）
- **不负责**：UI 展示、记忆存储、感知采集、LLM 调用
- **约束**：所有其他层只向人格层提供输入，最终输出由人格层统一决定

---

## 当前状态

| 组件 | 文件 | 状态 |
|------|------|------|
| 角色配置加载 | `src/personality/character.py` | 🔄 有雏形，schema 待固化 |
| 情绪状态机 | `src/personality/emotion.py` | 🔄 有雏形，测试不足 |
| Prompt 构建 | `src/personality/prompt_builder.py` | 🔄 有雏形，需集中组装逻辑 |
| 角色配置文件 | `characters/*/personality.yaml` | 🔄 有示例，格式待规范 |
| 台词 RAG | 未实现 | ⬜ 后置 |

---

## Phase 0：补齐主流程（当前优先）

目标：确保人格层各组件在主流程中稳定串联。

### 任务清单

- [ ] 确认 `character.py` dataclass 字段与当前 `personality.yaml` 一致，加字段校验
- [ ] 对缺字段的 YAML 给出明确错误提示（不能运行时 KeyError）
- [ ] 确认 `emotion.py` 的 `update()` 和 `decay()` 在主循环中被正确调用
- [ ] 确认 `prompt_builder.py` 是唯一拼接 system prompt 的地方，其他地方不允许直接拼接
- [ ] 运行 10 轮对话，用 `--debug` 输出检查情绪状态变化是否符合预期

### 验收

- YAML 缺字段时有友好报错，不出现运行时 AttributeError
- `--debug` 模式下每轮能看到当前情绪状态和构建的 prompt 摘要

---

## Phase 1：人格层定型

目标：把人格层从"能跑"提升到"稳定可控、可验证、可维护"。

### 1.1 角色配置 Schema 固化

设计目标配置格式（参考设计文档）：

```yaml
# characters/<name>/personality.yaml
name: ""
version: ""

personality:
  core_fear: ""       # 核心恐惧/弱点
  surface_trait: ""   # 外在表现
  hidden_trait: ""    # 内在真实

behavior_rules:
  - ""  # 具体行为约束，越精确越好

proactive_style:
  idle_too_long: ""
  user_working_late: ""
  user_gaming: ""

forbidden_words: []
verbal_habits: []

emotion_triggers:
  happy: []
  angry: []
  shy: []
  cold: []
```

任务：

- [ ] 用 Python dataclass 或 pydantic 定义完整 schema
- [ ] 写 `validate_character(config)` 函数，在加载时运行
- [ ] 所有字段加默认值，避免新角色缺可选字段报错

### 1.2 情绪状态机增强

首版只保留最少可验证状态：

```python
class EmotionState:
    mood: str          # normal / happy / angry / shy / cold
    persist_count: int # 情绪持续轮数，用于 decay
```

`affection`、`energy`、`last_interaction` 等在桌面阶段（Phase 4）再引入。

任务：

- [ ] 固化 `EmotionState` 数据结构，只保留 `mood` 和 `persist_count`
- [ ] `update(event)` 接受枚举事件类型（`user_praise` / `user_ignore` 等），而不是原始文本
- [ ] `decay()` 每轮对话后调用，`persist_count` 归零时 mood 回到 `normal`
- [ ] 触发词匹配逻辑从 `update()` 中单独提取为 `detect_event(user_input) -> event`
- [ ] 写单元测试：给定输入序列，验证情绪按预期变化和衰减

### 1.3 Prompt 构建集中化

任务：

- [ ] `prompt_builder.py` 暴露唯一入口 `build_system_prompt(character, emotion_state, memory_context) -> str`
- [ ] prompt 模板按角色分区：基础人格 / 当前情绪描述 / 行为约束 / 记忆上下文（可选）
- [ ] 增加 prompt 长度估算，超过阈值时记录警告
- [ ] 写单元测试：给定角色和情绪，验证 prompt 包含必要字段且不包含禁用词汇

### 1.4 人格稳定性日志指标

任务：

- [ ] 在会话日志中记录每轮的情绪状态（写入 JSONL 的 `emotion` 字段）
- [ ] `--replay` 模式下能展示每轮情绪变化轨迹
- [ ] 提供一个简单脚本 `tools/review_emotion.py`，从日志中统计情绪分布

### 验收

- 同一角色连续 20 轮对话，情绪变化轨迹可从日志中复核
- 明确触发词输入后，情绪变化在 1-2 轮内可预测
- `build_system_prompt` 单元测试通过

---

## Phase 2 之后：台词 RAG（后置）

台词检索增强（RAG）是人格层的锦上添花项，在人格稳定之后再引入。

```
角色台词数据 → 按情绪标签分类 + embedding → ChromaDB 本地存储
用户输入 → embedding → 检索同情绪标签台词 → 注入 prompt 作为 few-shot 示例
```

前置条件：

- 有足够干净的台词库（因角色而异，需单独整理）
- 长期记忆结构已稳定（避免与 ChromaDB 结构冲突）
- few-shot 数量保持极少，只作为风格校准，不能压过角色基础设定

任务（后置，Phase 3 之后）：

- [ ] 整理目标角色的台词数据，按情绪标签分类
- [ ] 用 `text-embedding-3-small` 向量化台词
- [ ] 接入 ChromaDB，按情绪标签分 collection 存储
- [ ] 在 `prompt_builder.py` 中增加 `rag_context` 可选注入点
- [ ] 验证 RAG 开启/关闭对输出风格的影响

---

## 依赖说明

| 依赖 | 来源 | 说明 |
|------|------|------|
| 记忆上下文 | 记忆层 | Phase 2 接入，Phase 1 可用占位空字符串 |
| 感知上下文 | 感知层 | Phase 3 接入，Phase 1 忽略 |
| LLM 调用 | 能力层 | 已有，`llm.py` 封装好 |

---

## 不建议在此阶段做的事

- 引入 `affection`、`energy` 等复杂情绪字段（Phase 4 之后）
- 接入 RAG（Phase 3 之后）
- 多角色并行切换（Phase 6）
