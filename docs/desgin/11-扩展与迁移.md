# 扩展与迁移

## 目标
定义 Kokoro 当前角色内容扩展和兼容迁移的真实边界: 角色目录结构、配置入口、显示资源入口，以及当前尚未实现的自动迁移边界。

## 范围
### 包含
- 角色目录 `characters/<character_id>/` 的当前入口文件约定。
- `personality.yaml` 与可选 `manifest.yaml` 的职责边界。
- 默认启动角色、角色切换、角色资源查找的当前行为。

### 不包含
- 不把插件系统、角色包安装器、自动 schema 迁移工具写成现状。
- 不定义远程下载、市场、签名校验或跨仓库分发机制。

## 外部锚点引用
- 02-人格层.md#输入 / 输出:人格配置字段。
- 09-3d-model-support.md#输入 / 输出:manifest 中 `model3d` 的字段。
- 06-UI层.md#约束与规则:显示偏好不写回角色包。
- 07-IPC协议.md#流程:角色切换协议。

## 输入 / 输出
### 输入
| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `characters/<character_id>/personality.yaml` | file | 是 | 角色人格配置入口。 |
| `characters/<character_id>/manifest.yaml` | file | 否 | 角色显示资源入口。 |
| `KOKORO_DEFAULT_CHARACTER` | string | 否 | 默认启动角色 ID。 |
| `/switch-character?name=...` | query | 否 | 运行时切换角色。 |
| `/admin/characters/{character_id}` PUT body | `{raw_yaml: string}` | 否 | 角色 YAML 编辑输入。 |
| `schema_version` | string | 否 | AIRI 合并修正(追加:2026-04-28): 角色配置版本；v2 用于角色卡字段和模块绑定。 |
| `PluginEvent` | object | 否 | AIRI 合并修正(追加:2026-04-28): future plugin 输入，第一阶段只允许 `notify`、`query_state`、`quick_action` 三类动作。 |

### 输出
| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `CharacterConfig` | dataclass | 是 | 人格配置加载结果。 |
| `display` 配置 | JSON | 否 | 从 `manifest.yaml` 派生的显示配置。 |
| 默认角色解析结果 | path / id | 否 | CLI 与 sidecar 启动时使用的角色入口。 |
| `CharacterCardExport` | file | 否 | AIRI 合并修正(追加:2026-04-28): 后续角色卡导出结果；第一阶段不要求完整 Character Card V3 兼容。 |

## 流程
1. 角色目录以文件夹名作为 `character_id`。
2. 启动时通过 `resolve_default_character_path()` 或 `KOKORO_DEFAULT_CHARACTER` 选择初始角色。
3. 人格层总是从 `personality.yaml` 读取角色配置。
4. 显示层若存在 `manifest.yaml`，则从中解析 `display.mode` 及资源入口。
5. 运行时角色切换通过 `/switch-character` 替换当前服务实例。
6. 管理界面编辑角色时直接提交 YAML 文本，再由后端写回文件并触发重载。
7. AIRI 合并修正(追加:2026-04-28): 角色 schema v2 迁移必须先兼容读取 v1，再逐步补齐 `identity`、`dialogue`、`modules`、`memory`、`proactive`；自动写回迁移必须由用户显式触发。
8. AIRI 合并修正(追加:2026-04-28): 插件协议必须在内部 NotifyEvent 稳定后开放；第一阶段插件只允许提交 notify、读取公开 state、响应 quick_action。

## 约束与规则
- `character_id` 由目录名决定，当前没有额外 manifest 级唯一标识。
- `personality.yaml` 是必需文件；`manifest.yaml` 是可选文件。
- 用户本地偏好、关系状态、记忆和日志存放在 `data/` 下，不写回 `characters/`。
- 当前没有自动 schema 迁移器；字段变化需通过文档和人工修正处理。
- 当前没有角色包导入导出 API。
- AIRI 合并修正(追加:2026-04-28): 新增角色通常只改 `characters/<id>/personality.yaml`、`manifest.yaml` 和资源文件；不得为单个 AIRI 角色改核心代码。
- AIRI 合并修正(追加:2026-04-28): Character Card 导入必须把外部字段映射到 Kokoro v2 schema；未支持字段写入 `[待定:外部角色卡字段未映射]` 列表，不得合并进自由文本 prompt。
- AIRI 合并修正(追加:2026-04-28): 插件协议不得开放原始文件系统路径、provider key、未过滤感知上下文或长期记忆直接写入能力。
- `[待定:角色 schema 自动迁移策略尚未定义]`。

## 验收标准
- 新建 `characters/<id>/personality.yaml` 且字段合法；服务可加载该角色。
- 缺少 `personality.yaml`；启动或切换角色时报错。
- 新建合法 `manifest.yaml`；`/state.display` 返回对应显示模式配置。
- 通过 admin 修改角色 YAML 并重载；当前角色配置被更新。
- 读取 v1 角色配置；服务加载成功且不自动改写原文件。
- 导入包含未支持字段的外部角色卡；导入报告列出 `[待定:外部角色卡字段未映射]`，生成的 `personality.yaml` 不包含整段外部 prompt。

## 待定汇总
- `[待定:角色 schema 自动迁移策略尚未定义]`。
- `[待定:外部角色卡字段未映射]`。

## Amendments
<!-- 变更记录,只追加,不改写。 -->

### 2026-04-28
- 变更:追加 AIRI 合并的角色 schema v2 迁移、Character Card 导入边界和插件最小协议。
- 原因:级联自 00-系统设计总览.md 的同日变更
- 影响:02-人格层.md / 04-感知层.md / 07-IPC协议.md / 10-安全与边界.md

### 2026-04-26
- 变更:按当前角色目录、默认角色解析、manifest 入口和 admin 编辑方式重写扩展与迁移文档。
- 影响:无
- 级联更新:09-3d-model-support.md、07-IPC协议.md
